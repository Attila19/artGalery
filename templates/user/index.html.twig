{% extends 'backoffice.html.twig' %}

    {% block titre %}
        {{ title }}
    {% endblock %}
    {% block title %}
        {{ parent() }} {{ title }}
    {% endblock %}
    
    
    
    {% block body %}
    
        {{ form(formUser) }}
    
        <table class="table table-dark table-striped mt-5 w-75 mx-auto">
            <thead>
            <tr>
                <th>Id</th>
                <th>Mot de passee</th>
                <th>Pseudo</th>
            </tr>
            </thead>
            <tbody>
    
            {% for user in users %}
                <tr>
                    <td >{{ user.id }}</td>
                    <td>{{ user.password }}</td>
                    <td >{{ user.nickname}}</td>
                    <td>
                        {# <a href="{{ path({'id': category.id}) }}" class="btn btn-info">Modifier</a>
                        <a onclick="return confirm('Etes-vous sûr?')" href="{{ path('city_delete', {'id': city.id}) }}" class="btn btn-danger">Supprimer</a> #}
    
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    
    
    {% endblock %}
    